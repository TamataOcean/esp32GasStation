#include "Observateur.h"

bool Observateur::observer() {
    /*WiFiClient espClient;
    PubSubClient client(espClient);
    client.subscribe("#");
    client.loop();*/
    return true;
}

void Observateur::push() {
    /*WiFiClient espClient;
    PubSubClient client(espClient);
    client.publish("topic", "payload");*/
}